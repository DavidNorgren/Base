#pragma once

#include <cstdlib>
#include <exception>
#include <functional>
#include <iostream>
#include <fstream>
#include <string>
#include <map>
#include <vector>

using std::string;
using std::wstring;
using std::function;
using std::exception;
using std::map;
template<typename T> using list = std::vector<T>;

using uint = unsigned int;
using uchar = unsigned char;